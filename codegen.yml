overwrite: true
schema: "http://localhost:4000"
documents: null
generates:
  src/resolvers-types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
        useIndexSignature: true
